{% extends 'BaseMainBundle::layout.html.twig' %}

{% block body -%}
    {% include "::messages.html.twig" %}

    <h1>{{ 'form.pages'|trans({}, 'StaticBundle') }}</h1>
    <a class="btn btn-default" href="{{ path("page_new") }}"><span class="glyphicon glyphicon-plus-sign"></span> {{ "form.create" | trans({}, 'StaticBundle') }}</a>

    {{ grid(grid, "BaseMainBundle:Grid:blocks.html.twig") }}
{% endblock %}