{% if pages|length > 0 %}
    <div class="static_pages">
        {% for entity in pages %}
            <div class="static_page">
                <h1>{{ entity.title }}</h1>

                {% if entity.text %}
                    {{ entity.text|raw }}
                {% endif %}
            </div>
        {% endfor %}
    </div>
{% endif %}